# Evaluator cache

Cache versioned evaluator definitions here only after they are verified against the
Microsoft Foundry evaluator catalog. Names in `../../eval.yaml` are candidates for
future synchronization, not remote evaluator claims.

