# Suite cache

Cache reviewed Microsoft Foundry evaluation-suite definitions here only after remote
lookup or registration succeeds. Use versioned filenames and record the matching
references under `evaluationSuites` in the selected metadata environment.

This folder is not the source of local evaluation intent; that is `../../eval.yaml`.

