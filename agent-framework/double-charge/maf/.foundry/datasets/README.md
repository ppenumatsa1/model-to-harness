# Dataset cache

Cache reviewed dataset references or sanitized local copies here only after the
corresponding Microsoft Foundry dataset has been verified. Do not store production
conversations, credentials, raw prompts, or unrestricted tool payloads.

The deterministic local runner reads the root shared-package fixtures directly.

