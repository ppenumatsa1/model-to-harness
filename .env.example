# Root local-development PostgreSQL dependency only.
# Each framework owns its DATABASE_URL and migrations and uses its own schema.
POSTGRES_USER=model_to_harness
POSTGRES_PASSWORD=local-development-only
POSTGRES_DB=model_to_harness
POSTGRES_PORT=5432
